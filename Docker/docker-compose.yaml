x-volumes:
  config: &config /home/glogware/GLogWare2/CompanyCity/Config:/app/Config
  logfiles: &logfiles /home/glogware/GLogWare2/CompanyCity/Logfiles:/app/Logfiles

x-tz-env: &tz-env
  TZ: Europe/Zurich

services:
  bridgemanager-op7100br:
    image: bridgemanager:latest
    container_name: BridgeManager-OP7100BR
    environment:
      OP: OP7100BR
      <<: *tz-env
    volumes:
      - *config
      - *logfiles
  bridgemanager-op7200br:
    image: bridgemanager:latest
    container_name: BridgeManager-OP7200BR
    environment:
      OP: OP7200BR
      <<: *tz-env
    volumes:
      - *config
      - *logfiles
